const enhancer = require('./enhancer.js');

const bat1 = {
  name: 'Rickenbacker 4001',
  durability: 80,
  enhancement: 18
}

describe("Sanity check", () => {
  it('Math', () => {
    expect(5 + 5).toBe(10);
  })
})